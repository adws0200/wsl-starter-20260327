services:
  node-app:
    build:
      context: .
      dockerfile: Dockerfile.node
    ports:
      - "3000:3000"

  py-app:
    build:
      context: .
      dockerfile: Dockerfile.py
    ports:
      - "8000:8000"
